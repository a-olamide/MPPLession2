package lesson2.lecture.dependency_example;

public class RightTriangle {
    public double computeHypothenuseLength(double base, double height) {
        return Math.sqrt(base*base + height*height);
    }
}
